# OpenMake Legal Pack

법무 분야 전문 스킬 5종(컴플라이언스·기업법무·형사·노무·특허). **무료**로 쓸 수 있습니다.

## 설치
OpenMake → 설정 → 확장에서 이 리포 URL 로 설치합니다. public 리포라 토큰이 필요 없습니다.

## 구성
- `openmake-addon.json` — add-on 매니페스트(권한 선언 없음 = 네트워크·DB 미사용)
- `skills/<id>/SKILL.md` — 스킬 본문
